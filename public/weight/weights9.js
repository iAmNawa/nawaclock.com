var numbas = 0;
var plates = [90, 70, 50, 20, 10, 5];

function whichPlates(input) {
  for (var i = 0; i < plates.length; i++) {
    while (numbas < 5 ) {

    }

}
}
console.log(whichPlates(200));





a = 90
b = 70

weight to put = total

while (total > 5) {
  if total - a > 90) then total = total - a
  add 1 to first element of array
}
(total -= a or total -= b)
